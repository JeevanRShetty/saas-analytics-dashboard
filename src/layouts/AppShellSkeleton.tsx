/**
 * AppShellSkeleton.tsx
 *
 * Re-exported from here so layouts/index.ts is the single import point.
 * The actual implementation lives in shared/components/ui/Skeleton.tsx
 * since it's used by the auth guard before the app shell mounts.
 */

export { AppShellSkeleton } from '@/shared/components/ui/Skeleton'